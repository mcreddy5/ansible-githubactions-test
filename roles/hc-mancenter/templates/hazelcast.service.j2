[Unit]
Description=hazelcast
Before=
After=network.target

[Service]
ExecStart={{ hc_installation_dir }}/hazelcast-{{ hc_version }}/bin/hz-start 
ExecStop={{ hc_installation_dir }}/hazelcast-{{ hc_version }}/bin/hz-stop 
Restart=on-failure

[Install]
WantedBy=multi-user.target